---
$schema: ../../../reviewer/output-schema.json
audit_period: 2026-05
generated_at: 2026-06-19T00:00:00Z
generated_by: /acme:audit-monthly@0.2.0 (dry-run — estágio 1, coleta Claude)
sample_pct: null            # sem amostragem: 0 runs de produção no mês fechado
total_subscriptions_audited: 14
total_runs_in_month: 0      # produção, mês fechado 2026-05
sample_size: 0
constitution_version: 0.4.0
manifest_version: 0.22.1
forge_command_version: audit-monthly@0.2.0
project_type: agentic_saas
deep_agent_ready: true
data_provenance:
  production_db: read-only (role aicfo_audit) — MonthlyAnalysis em 2026-05 = 0
  langsmith: project="Aicfo", read-only — 2026-05 = 0 runs; runtime existe só em 2026-06, originado de execuções de desenvolvimento/teste/staging + evals (NÃO produção, NÃO um ambiente "dev"). Todos os ambientes compartilham o mesmo projeto LangSmith — sem separação.
---

## Sumário executivo

Auditoria de **dry-run** do estágio 1 (coleta Claude) para o mês fechado **2026-05**. O produto está **pré-lançamento** (ainda não aberto a clientes), portanto **não há outcomes de produção** em 2026-05: 0 `MonthlyAnalysis` no banco e 0 runs no LangSmith dentro da janela do mês. As 14 subscriptions `active/assisted` são cadastros nominais que herdaram o default do schema, sem entrega real.

O sinal de runtime existente vive em **2026-06** e vem de **execuções de desenvolvimento/teste/staging + evals** (não-produção) — 4.805 runs traçados, ~219 análises full. Não é um ambiente "dev" (o branch `dev` foi descontinuado em 2026-06-19); são runs ad-hoc que tracejam no mesmo projeto LangSmith. A auditoria reprodutível de mês fechado é deliberadamente vazia de runtime; o valor desta rodada é **estrutural (C1–C8) + prontidão de lançamento**. A telemetria de junho entra como **anexo informativo** (não como métrica de produção) e já revela dois bloqueadores de confiabilidade a corrigir antes do go-live.

## Findings críticos

1. `{principle: reliability, severity: critical, node: dre-narrative, evidence: langsmith 2026-06}` — nó **`dre-narrative` com 6/6 runs em erro (100%)**, 0 tokens. Nó aparentemente quebrado. **Bloqueador de lançamento.**
2. `{principle: reliability, severity: high, node: normalization, evidence: langsmith 2026-06}` — **`normalization` com 240/607 erros (39,5%)**. Taxa de falha incompatível com entrega ao cliente. **Bloqueador de lançamento.**
3. `{principle: C6, severity: medium, evidence: docs/forge/project.json + docs/onda-0/sla_threshold.md}` — **drift de documentação de telemetria**: ambos declaram provider `langfuse`, mas o código (`src/observability/tracing.ts`, `package.json`) usa **LangSmith**. O trace funciona; a doc é que está desalinhada com a Constitution C6 (LangSmith canônico).

## Auditoria por princípio

### C1 — Diagnose-before-design
- SKU `monthly-analysis` tem `spec.md` ([src/skus/monthly-analysis/spec.md](../../../src/skus/monthly-analysis/spec.md)). ✅
- Findings: nenhum.

### C2 — Outcome-first
- Cláusula de outcome presente na spec (§1 "Cláusula de outcome (C2)") e em [docs/onda-0/sku_piloto.md](../../onda-0/sku_piloto.md). Outcome cobrável: "1 análise mensal entregue + aceita". ✅
- Findings: nenhum estrutural. (Verificação de `outcome_clause_hash` prod↔spec n/a — sem runs de produção.)

### C3 — Cost ≤ 25%
- Baseline-cost documentado: [docs/clients/aicfo/baseline-cost-monthly-analysis-2026-05-25.md](../../clients/aicfo/baseline-cost-monthly-analysis-2026-05-25.md).
- Custo observado (junho, dev): **R$ 9,66/mês total**; por análise **p50 R$ 0,003 / p95 R$ 0,069 / max R$ 0,40**. Trivial vs qualquer preço de PME → C3 **folgadíssimo**.
- ⚠️ Custo **aproximado**: os traces não expõem o nome do modelo (`models_seen: {unknown: 1749}`), então o coletor usou preço default. Limitação da tooling, não do produto.
- Findings: nenhum (info: instrumentar `ls_model_name` no trace para custo exato).

### C4 — SHADOW antes de cobrar
- Thresholds contratados em [docs/onda-0/sla_threshold.md](../../onda-0/sla_threshold.md): latência p95 <5min (SHADOW/ASSISTED), agreement por modo, ≥99% trace.
- Latência raiz junho **p95 ~80s** — bem dentro do teto de 5min. ✅
- ⚠️ **Prontidão**: `Subscription.mode` tem **default `assisted`** no schema; as 14 subs ativas estão em `assisted` sem terem passado por SHADOW real (pré-lançamento, modo nominal). Ao abrir para clientes, garantir que a promoção SHADOW→ASSISTED passe pelos 5 gates — o default não deve virar entrega automática.
- Findings: 0 breach (sem produção); 1 warning de governança de modo.

### C5 — Three-tier context (L0/L1/L2)
- Skills organizadas em `.claude/skills/{L0,L1,L2}` com hierarquia respeitada. ✅
- Cross-tier reads detectados: 0.
- Findings: nenhum.

### C6 — Telemetry
- Tracing **ativo e funcional**: 4.805 runs traçados em junho (auto-instrumentação LangChain/LangGraph → LangSmith). Cobertura efetiva alta no caminho do grafo.
- ❗ **Drift de doc** (ver finding crítico #3): `project.json` e `sla_threshold.md` dizem `langfuse`; canônico real = **LangSmith**. Corrigir os docs.
- ❗ **Sem separação de ambiente** (finding #4): local, staging, eval e produção compartilham o projeto LangSmith `Aicfo` (`project_name` só vem de `LANGSMITH_PROJECT`, sem tag de env — ver [src/observability/tracing.ts](../../../src/observability/tracing.ts)). Pós-launch, a amostragem da auditoria **não conseguirá isolar produção** de ruído de teste, corrompendo agreement/custo/latência.
- Findings: 2 medium (doc drift; falta de separação de ambiente).

### C7 — Portability
- Imports de SDK (`@anthropic-ai`, `openai`, `@google/genai`) fora de `src/llm/`: **0** (lint regex). ✅
- Findings: nenhum.

### C8 — Anti-customização heroica
- Hardcode por tenant (`tenantId === '...'`, `switch(tenantName)`) em `src/skus`/`src/skills`/agentes: **0**.
- Pastas `clients/`/`tenants/` em `src/`: **0**. ✅
- Findings: nenhum.

## Sample analysis (5-10%)
- **N/A** — 0 runs de produção no mês fechado (pré-lançamento). Sem pares humano/agente para concordância.

## Drift signals
| fonte | drift | severidade |
|---|---|---|
| project.json / sla_threshold.md | provider `langfuse` declarado ≠ LangSmith no código | medium |
| node dre-narrative | 100% erro em dev (junho) | critical |
| node normalization | 39,5% erro em dev (junho) | high |

## Anexo — telemetria de desenvolvimento/teste/staging (2026-06) — NÃO é produção
> Runs ad-hoc (local + staging + evals) que tracejam no projeto LangSmith `Aicfo`. Não é um ambiente "dev" nem outcome de cliente. Incluída para prontidão.

- Volume: 4.805 runs (llm 1.749 / chain 2.896 / tool 160); ~219 análises full "LangGraph".
- Erro geral: **8,74%** (420/4.805). Concentrado em `normalization` (39,5%) e `dre-narrative` (100%).
- Latência raiz: p50 2,6s / p95 ~80s / max 152s (população mista full + sub-runs de nó).
- Top latência por nó (p95): `financial-qa-review` 41,5s · `narrative-synthesis` 28,6s · `action-planning` 27,3s · `normalization` 24,7s.

## Recomendações
1. **Bloqueador**: investigar e corrigir `dre-narrative` (6/6 falhas) e `normalization` (39,5% erros) antes de abrir a clientes.
2. **C6 (doc)**: atualizar `docs/forge/project.json` e `docs/onda-0/sla_threshold.md` de `langfuse` → `langsmith` (ou abrir ADR se houver intenção de migrar o tracer).
3. **C6 (ambiente)** — **pré-requisito para auditorias futuras**: separar tracing por ambiente antes do launch — projeto LangSmith distinto para produção (ex: `LANGSMITH_PROJECT=aicfo-prod`) ou tag de env no metadata do trace, para a amostragem isolar produção de teste/staging.
4. **C4 governança**: revisar o default `assisted` de `Subscription.mode` — considerar default `shadow` até promoção formal, evitando entrega automática no go-live.
5. **Tooling**: instrumentar `ls_model_name`/modelo no metadata do trace para custo exato (hoje cai no preço default).
6. **Processo**: após o lançamento, rodar este audit sobre o **primeiro mês fechado com produção** para ativar amostragem de concordância (C4) e custo real por outcome (C3).

## Auto-rollbacks executados
Nenhum (`auto_rollback_on_breach=false`; sem produção).

---
> Estágio 2 (julgamento independente): este artefato é `deep_agent_ready` — pronto para o reviewer cross-LLM (Codex/DeepAgent via `forge-auditor`) validar contra `reviewer/validation-rules.json`.
