# Anthropic (fallback)
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Google Gemini (provider primário — classification, dre-narrative, action-plan)
GOOGLE_API_KEY=AIzaxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Modelo local (Fase 2 — ollama ou llama.cpp server)
LOCAL_LLM_BASE_URL=http://localhost:11434/v1
LOCAL_LLM_MODEL=qwen2.5:3b

# Langfuse (telemetria — C6)
LANGFUSE_PUBLIC_KEY=pk-lf-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
LANGFUSE_SECRET_KEY=sk-lf-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
LANGFUSE_HOST=https://cloud.langfuse.com

# Postgres (Prisma)
DATABASE_URL=postgresql://aicfo:aicfo@localhost:5432/aicfo?schema=public

# Redis (BullMQ)
REDIS_URL=redis://localhost:6379

# Server
PORT=3000
NODE_ENV=development

# JWT (auth-tenant — Onda 0)
JWT_SECRET=replace-with-strong-random-secret
JWT_EXPIRES_IN=7d

# Stripe (billing — Onda 0)
STRIPE_SECRET_KEY=sk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
STRIPE_WEBHOOK_SECRET=whsec_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Forge bypass (auditado — usar com critério; default vazio = sem bypass)
ACME_FORGE_BYPASS=
