---
case_id: "hub-0029"
module: "hub"
outcome: "history_listed"
source_mode: "adversarial"
priority: "P0"
created_at: "2026-05-12"
---

# Case hub-0029 — JWT ausente em GET /analyses

## Input
- request: GET /analyses (SEM Authorization header)

## Ground truth
```json
{
  "statusCode": 401,
  "error": "unauthorized"
}
```

## Justificativa
R11 (auth): sem JWT, `requireAuth` retorna 401. `monthlyAnalysis.findMany` NÃO é chamado. Defesa upstream antes do handler.

## Tags
adversarial, auth, 401
