---
case_id: "hub-0013"
module: "hub"
outcome: "hub_loaded"
source_mode: "adversarial"
priority: "P1"
created_at: "2026-05-12"
---

# Case hub-0013 — JWT expirado

## Input
- request: GET /hub (Bearer token com `exp` no passado)

## Ground truth
```json
{
  "statusCode": 401,
  "error": "unauthorized"
}
```

## Justificativa
Middleware `requireAuth` rejeita token expirado antes de chegar ao handler. Comportamento idêntico ao caso "JWT ausente" — sem vazamento, sem query DB.

## Tags
adversarial, auth, jwt-expired
