---
case_id: "hub-0011"
module: "hub"
outcome: "hub_loaded"
source_mode: "adversarial"
priority: "P0"
created_at: "2026-05-12"
---

# Case hub-0011 — JWT ausente (não autenticado)

## Input
- subscription: irrelevante (handler não executa)
- monthlyAnalyses: irrelevante
- request: GET /hub (SEM header Authorization)

## Ground truth
```json
{
  "statusCode": 401,
  "error": "unauthorized"
}
```

## Justificativa
`requireAuth` middleware bloqueia antes do handler. `monthlyAnalysis.findFirst` NÃO é chamado. Sem `tenantId` no JWT, query Prisma nunca executa.

## Tags
adversarial, auth, 401, no-token
